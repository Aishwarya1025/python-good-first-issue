try:
    n = int(input())
    if n < 0:
        print("Factorial not defined for negative numbers")
    else:
        fact = 1
        for i in range(1, n + 1):
            fact *= i
        print(fact)
except ValueError:
    print("Invalid input")
